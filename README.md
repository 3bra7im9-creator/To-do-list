# To do list
A simple and clean To-Do List web application that allows users to add, delete, and manage their daily tasks. The app saves all tasks automatically using localStorage, so the list stays even after closing the browser.
